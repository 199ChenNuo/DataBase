#include "RBTree.h"

using namespace std;
/*
int main() {
	RBTree tree(0, 0);

	for (int i = -3; i < 5; ++i) {
		cout << endl << "Temporary tree" << endl;
		tree.add(i, i);
	}

	tree.print();

	for (int i = -4; i < 9; i += 1) {
		cout << endl << "Temporary tree" << endl;
		tree.remove(i);
		tree.print();
	}

	for (int i = -2; i < 5; ++i) {
		tree.printSingle(tree.fetch(i));
		cout << endl;
	}

	tree.printSingle(tree.getRoot());
	system("pause");
}
*/